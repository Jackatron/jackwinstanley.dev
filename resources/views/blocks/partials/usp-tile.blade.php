<div class="flex items-center gap-6 text-sm sm:text-base">
    <img src="{{ $icon }}" alt="{{ $alt }}"/>
    <div class="leading-5">
        <p class="font-semibold text-primary">{{ $title }}</p>
        <p class="text-dark-blue text-sm">{{ $subtitle }}</p>
    </div>
</div>